query collateralInputs (
    $limit: Int
){
    collateralInputs(
        limit: $limit
    ) {
        address
        sourceTransaction {
            hash
        }
        value
        transaction {
            hash
        }
    }
}