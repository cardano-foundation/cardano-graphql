query utxoSetForAddress (
    $address: String!
){
    utxos( # Todo: Order by index
        order_by: { address: asc }
        where: { address: { _eq: $address }
        }
    ) {
        address
        value
    }
}