
query cardanoDynamic {
  cardano {
    tip {
      number
    }
    currentEpoch {
      blocks (
        limit:1,
        order_by: {
          number: desc_nulls_last
        }
      ) {
        slotInEpoch
      }
      lastBlockTime
      number
    }
  }
}
