schema: ./test/api/schema.graphql
overwrite: true
generates:
  ./test/api/src/graphql_types.ts:
      config:
        useIndexSignature: true
      plugins:
        - typescript
        - typescript-resolvers
