table:
  name: drep_hash
  schema: public
configuration:
  column_config: {}
  custom_column_names: {}
  custom_root_fields:
    select: drepHash
    select_aggregate: drepHash_aggregate
select_permissions:
  - role: cardano-graphql
    permission:
      columns:
        - view
        - has_script
        - raw
      filter: { }
      allow_aggregations: true
